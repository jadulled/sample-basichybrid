(function () {
    "use strict";
    /*
   hook up event handlers 
 */

    function register_event_handlers() {


        /* button  Alert */
        $(document).on("click", ".uib_w_3", function (evt) {
            /* your code goes here */
            function alertDismissed() {
                // do something
            }
            try {
                navigator.notification.alert(
                    'This alert is generated by the Cordova Dialogs plugin', // message
                    alertDismissed, // callback
                    'You did it!', // title
                    'Dismiss' // buttonName
                );
            } catch (e) {}
            /* your code goes here */
            //         try {
            //        if(intel.xdk.device.platform == "iOS"){
            //            alert("The Notification object's beep method may not execute on iOS devices.");
            //        }
            ////        navigator.notification.beep(1);
            ////        console.log("I beeped");
            //    } catch (e) {}
        });

        /* button  Vibrate */
        $(document).on("click", ".uib_w_4", function (evt) {
            /* your code goes here */
            try {
                navigator.notification.vibrate(1000);
            } catch (e) {}
        });

        /* button  Play Sound */
        $(document).on("click", ".uib_w_5", function (evt) {
            /* your code goes here */

            function alertDismissed() {
                // do something
            }
            if (intel.xdk.device.platform.toLowerCase() == "android") {
                try {
                    //                            alert("The Player object's playSound method may not execute on Android devices.");            
                    navigator.notification.alert(
                        "The Player object's playSound method may not execute on Android devices.", // message
                        alertDismissed, // callback
                        'Oops!', // title
                        'Dismiss' // buttonName
                    );
                } catch (e) {}
            }
            try {
                intel.xdk.player.playSound(intel.xdk.webRoot + "sounds/dog_bark.wav");
            } catch (e) {}



            //            function getWebPath() {
            //                "use strict";
            //                var path = window.location.pathname;
            //                path = path.substring(0, path.lastIndexOf('/'));
            //                return 'file://' + path;
            //            }
            //
            //            function getWebRoot() {
            //                "use strict";
            //                var path = window.location.href;
            //                path = path.substring(0, path.lastIndexOf('/'));
            //                return path;
            //            }
            //               function mediaSuccess() {
            //                console.log("Media success ");
            //            }
            //                     function mediaError() {
            //                console.log("Media error ");
            //            }
            //                     function mediaStatus() {
            //                console.log("Media status ");
            //            }
            //            var x = window.device && window.device.platform;
            //            console.log("platform = ", x);
            //
            //            if (x.match(/(ios)|(iphone)|(ipod)|(ipad)/ig)) {
            //                var media = new Media("sounds/dog_bark.wav", mediaSuccess, mediaError, mediaStatus);
            //            } else {
            //                var media = new Media(getWebRoot() + "/sounds/dog_bark.wav", mediaSuccess, mediaError, mediaStatus);
            //            }
            //
            //            console.log("media.src = ", media.src);
            //            media.play();


        });

        /* button  Play Dead */
        $(document).on("click", ".uib_w_6", function (evt) {
            /* your code goes here */
            document.getElementById("shroud").style.display = "block";
        });

    }
    document.addEventListener("app.Ready", register_event_handlers, false);
})();